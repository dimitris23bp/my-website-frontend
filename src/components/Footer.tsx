import { Box } from "@mui/material";

const Footer = () => {
    return (
        <Box
            sx={{
                bgcolor: "orange",
                height: "100px",
                bottom: 0,
                width: "100%",
            }}
        >
            Footer
        </Box>
    );
};

export default Footer;
